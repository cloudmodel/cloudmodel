<%= model.passenger_env %>:
  url: redis://mymaster
  sentinels:
    <% model.deploy_redis_sentinel_set.sentinel_hosts.each do |host| -%>
    - 
      host: <%= host['ip'] %>
      port: <%= host['port'] %>
    <% end -%>
  role: master