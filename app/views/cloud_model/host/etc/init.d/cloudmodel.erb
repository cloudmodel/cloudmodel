#!/sbin/runscript

depend() {
  before net
  use logger
}

start() {
  ebegin "Starting CloudModel"
    <%=raw firewall_worker.start_script %>
  eend $?
}

stop() {
  ebegin "Stopping CloudModel"
    <%=raw firewall_worker.stop_script %>
  eend $?
}
