## Check nginx-Status
## This plugin test the nginx service on the specified host
# check_smart -d <device>

define command {
    command_name    check_nginx
    command_line    $PLUGINSDIR$/check_nginx.rb -h $HOSTADDRESS$
}