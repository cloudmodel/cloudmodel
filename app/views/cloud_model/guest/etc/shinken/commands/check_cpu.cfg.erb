## Check cpu usage
## This plugin tests cpu usage

define command {
    command_name    check_cpu
    command_line    $PLUGINSDIR$/check_cpu.rb -w $_HOSTCPU_WARN$ -c $_HOSTCPU_CRIT$ -h $HOSTADDRESS$
}