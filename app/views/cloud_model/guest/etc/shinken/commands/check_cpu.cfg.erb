## Check cpu usage
## This plugin tests cpu usage

define command {
    command_name    check_cpu
    command_line    $PLUGINSDIR$/check_cpu.rb -w $_HOSTCHECK_CPU_WARN$ -c $_HOSTCHECK_CPU_CRIT$ -h $HOSTADDRESS$
}