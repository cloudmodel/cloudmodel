## Check LVM usage
## This plugin tests the LVM service on the specified host for given volume group

define command {
    command_name    check_lvm
    command_line    $PLUGINSDIR$/check_lvm.rb -w 90 -c 95 -d "$_HOSTLVM_GROUPS$" -h $HOSTADDRESS$
}