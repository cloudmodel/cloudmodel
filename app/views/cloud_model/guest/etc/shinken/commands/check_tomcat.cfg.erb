## Check Tomcat-Status
## This plugin test the Tomcat service on the specified host

define command {
    command_name    check_tomcat
    command_line    $PLUGINSDIR$/check_tomcat.rb -h $HOSTADDRESS$
}